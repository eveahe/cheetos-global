import React from "react";
import "./Footer.css";

// Setting the content of the footer
const Footer = props => (
  <footer>
    <p>All cheetos and assorted cheesy puffs are (sadly) not my property, but property of their respective chip company owners.</p>
  </footer>
);

export default Footer;